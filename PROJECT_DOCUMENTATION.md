# NeuroDeck — Полная документация проекта

> **NeuroDeck** — геймифицированный трекер привычек в стиле Dark Souls, реализованный как Telegram Mini App. Игрок — узник подземелья, чьё единственное оружие против тьмы — собственная дисциплина.

---

## Оглавление

1. [Обзор проекта](#1-обзор-проекта)
2. [Технологический стек](#2-технологический-стек)
3. [Структура файлов](#3-структура-файлов)
4. [Игровые механики](#4-игровые-механики)
   - [4.1 Карточки (Колода)](#41-карточки-колода)
   - [4.2 Ранги и мастерство](#42-ранги-и-мастерство)
   - [4.3 Система адаптации](#43-система-адаптации)
   - [4.4 Герой и атрибуты](#44-герой-и-атрибуты)
   - [4.5 Уровни и XP](#45-уровни-и-xp)
   - [4.6 Боссы подземелья](#46-боссы-подземелья)
   - [4.7 Цели](#47-цели)
   - [4.8 Инвентарь и артефакты](#48-инвентарь-и-артефакты)
   - [4.9 Карта подземелья](#49-карта-подземелья)
   - [4.10 Система Hollow / Искупление](#410-система-hollow--искупление)
   - [4.11 Эстус](#411-эстус)
5. [Ежедневные циклы](#5-ежедневные-циклы)
6. [Статистика и достижения](#6-статистика-и-достижения)
7. [Синхронизация данных](#7-синхронизация-данных)
8. [Атмосфера и визуал](#8-атмосфера-и-визуал)
9. [Аудио и тактильная отдача](#9-аудио-и-тактильная-отдача)
10. [Уведомления](#10-уведомления)
11. [Онбординг](#11-онбординг)
12. [Жесты и навигация](#12-жесты-и-навигация)
13. [Сохранение данных](#13-сохранение-данных)
14. [Схема всех взаимодействий](#14-схема-всех-взаимодействий)
15. [Ключевые константы](#15-ключевые-константы)

---

## Обновление 2026-08-25 (баланс v2 и техдолг)

- **XP-кривая v2:** пороги до 15 уровня [50,100,200,380,700,1300,2400,4500,8500,16000,22000,30000,40000,52000,68000], далее ×1.65 (путь 10→15 ≈ 228k вместо 774k).
- **Боссы:** HP масштабируется ×(1 + floor((level-1)/5)×0.25); щит Химеры — 3 удара (было 5).
- **Ярость обязательна:** атака невозможна при rage>0; непотраченная ярость в понедельник превращается в урон HP (8+stage×4 за очко, HP не ниже 1, возможен Hollow).
- **Цели:** не дают очков действий (эксплойт goal.dmg закрыт); +1 очко атрибута за выполненную цель (не за шаг); XP цели делится пополам — по шагам и за завершение.
- **Лут:** при исчерпании пула артефактов начисляются осколки (+3 за убийство босса, +2 за прок завершения), видны в рюкзаке.
- **Кровавая клятва:** награда 500 × (индекс ранга +1); удаление карты клятвы проваливает её.
- **Эволюции:** реализованы все три — глубина (мастерство ×1.5), частота (+1 стат-очко за завершение), стабильность (шанс волевой проверки ×2 при скипе).
- **Престиж:** бонус XP ограничен ×1.5 суммарно.
- **Серии:** сбрасываются в 0, если карта пропущена полные сутки; failCard — не чаще раза в сутки на карту.
- **Hollow:** наступает на 1 HP (ход босса или понедельничный урон ярости); искупление — 3 идеальных дня → 30% HP.
- **Telegram MiniApp:** добавлен init (ready/expand/цвета хедера); чистая установка получает стартовую колоду и онбординг без запросов восстановления (флаг `neurodeck_ever_saved`).
- **Производительность:** Pixi ticker останавливается вне экрана босса; grain/torch/particles кэшируются текстурами; pixi-filters CDN убран.
- **Сохранения:** схема v5 с миграциями; ошибки облачных чанков прерывают запись меты; разрешение конфликтов по timestamp; cardHistory обрезается до 120 дней; share-ссылки вырезают payload истории.
- **Боевка 2.0 (Crucible):** босс-ран из 3 боёв, телеграфы интентов, 4 действия героя + стиль деки, фляги за осколки, лавка; баланс через Godot-полигон (`tools/combat-lab`, baseline v1).

---

## 1. Обзор проекта

**NeuroDeck** — приложение, в котором геймификация трекера привычек построена вокруг RPG-механик Souls-like жанра:

- Выполняешь привычку → наносишь урон боссу, получаешь XP
- Пропускаешь привычку → босс лечится, ты теряешь HP
- HP достигает 0 → становишься «Полым» (дебафф -50% урона)
- Повышаешь ранг карточки → растут атрибуты героя
- Накапливаешь ранг-апы → открываешь новые комнаты на карте подземелья
- Побеждаешь босса → получаешь награды, появляется новый босс

**Развёртывание:** GitHub Pages → `https://serwaksus.github.io/neurodeck/`
**Платформа:** Telegram Mini App (BotFather)
**Язык интерфейса:** русский

---

## 2. Технологический стек

| Компонент | Технология |
|-----------|-----------|
| Разметка | Чистый HTML5 |
| Стили | Чистый CSS3 (без препроцессоров) |
| Логика | Чистый Vanilla JS (ES5/ES6, без фреймворков) |
| Анимации | CSS Keyframes + Canvas 2D API |
| Звук | Web Audio API (синтез 8-bit тонов) |
| Тактильность | Telegram WebApp HapticFeedback |
| Хранилище | localStorage + IndexedDB + Telegram CloudStorage |
| Платформа | Telegram Mini App |
| Внешние библиотеки | `telegram-web-app.js` (единственная) |

**Cache-busting:** версии `?v=N` в URL CSS/JS (текущая `?v=23`) — Telegram WebView агрессивно кэширует.

---

## 3. Структура файлов

```
neurodeck/
├── index.html          (~450 строк) — разметка, модалки, вкладки
├── css/
│   └── style.css       (~760 строк) — все стили, анимации, адаптив
├── js/
│   └── app.js          (~2750 строк) — вся логика приложения
└── PROJECT_DOCUMENTATION.md — этот файл
```

### HTML структура (основные блоки):

```
<div class="app-wrap">
  ├── Атмосферные слои:
  │   ├── .dungeon-bg          — тёмный фон подземелья
  │   ├── .light-rays          — лучи света
  │   ├── #mistLayer           — SVG туман (2 слоя fractalNoise)
  │   ├── #dustCanvas          — canvas с частицами пыли
  │   ├── .torch-flicker       — мерцание факелов
  │   └── #particles           — canvas для burst-эффектов
  │
  ├── Оверлеи:
  │   ├── #lvlOverlay/#lvlBanner       — анимация повышения уровня
  │   ├── #rankupOverlay/#rankupBanner — анимация ранг-апа
  │   ├── #spiritMsg                   — сообщения Духа подземелья
  │   ├── #toast                       — всплывающие уведомления
  │   └── #tooltip                     — тултипы предметов
  │
  ├── Модальные окна:
  │   ├── #goalModal       — создание цели
  │   ├── #forgeModal      — ковка карточки
  │   ├── #editCardModal   — редактирование карточки (после ранг-апа)
  │   ├── #syncModal       — синхронизация и настройки
  │   ├── #roomDetailModal — детали комнаты на карте
  │   └── #confirmOverlay  — универсальный диалог подтверждения
  │
  ├── <div class="app"> (shakeWrap)
  │   ├── .header          — лого, кнопка синхры, мини-герой, слайдер побега
  │   ├── .main
  │   │   ├── nav.tabs     — навигация (6 вкладок)
  │   │   └── main.content — 6 секций:
  │   │       ├── #view-deck  — Колода карточек
  │   │       ├── #view-hero  — Герой (статы + цели)
  │   │       ├── #view-inv   — Инвентарь (paperdoll + рюкзак)
  │   │       ├── #view-boss  — Босс (крупный вид)
  │   │       ├── #view-map   — Карта подземелья
  │   │       └── #view-stats — Статистика
  │   ├── aside.boss-panel — боковая панель босса (мини)
  │   └── footer.bottom-nav — нижняя навигация (мобильная)
  │
  └── <script> telegram-web-app.js + app.js
```

---

## 4. Игровые механики

### 4.1 Карточки (Колода)

**Карточка** — основная единица привычки в игре. Каждая карточка представляет собой ежедневное задание.

**Свойства карточки:**
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | number | Уникальный ID |
| `name` | string | Название (напр. «Медитация») |
| `meta` | string | Метаданные вида `🧠 15 мин · утро` |
| `stat` | string | Привязанный стат: `str`/`end`/`int`/`cha`/`wil`/`agi` |
| `rank` | string | Текущий ранг (C → SSS) |
| `mastery` | number | Текущее мастерство |
| `masteryThreshold` | number | Порог мастерства до след. ранга |
| `streak` | number | Текущий стрик (дни подряд) |
| `totalCompletions` | number | Всего выполнений за всё время |
| `daysActive` | number | Дней с первого выполнения |
| `firstCompletedAt` | number | Timestamp первого выполнения |
| `lastCompletedAt` | number | Timestamp последнего выполнения |

**Действия с карточкой:**
- **✓ Выполнить** — даёт XP, мастерство, урон боссу, шанс лута
- **✕ Пропустить** — босс лечится, стрик обнуляется
- **✎ Редактировать** — открыть форму редактирования (long-press на мобилке)
- **🗑 Удалить** — удалить с подтверждением

**Ковка карточки** (`forgeCard()`):
- Игрок выбирает: название, стат, время суток, длительность, **стартовый ранг (все 12: C→SSS)**, порог мастерства
- Стартовый ранг можно задать любой — полезно при восстановлении после потери данных
- При создании: +10 XP, эффект частиц, звук ковки
- Карточка добавляется в начало массива `FORGED`

**3D Tilt-эффект:** карточки наклоняются в 3D при движении мыши/касании (`perspective(800px) rotateX/rotateY`).

---

### 4.2 Ранги и мастерство

**Прогрессия рангов (12 ступеней):**
```
C → CC → CCC → B → BB → BBB → A → AA → AAA → S → SS → SSS
```

**Цвета по рангам:**
| Базовый ранг | Цвет | Палитра |
|-------------|------|---------|
| C/CC/CCC | Серый | `#9ca3af` |
| B/BB/BBB | Синий | `#60a5fa` |
| A/AA/AAA | Фиолетовый | `#c084fc` |
| S/SS/SSS | Золотой | `#fbbf24` |

**Фразы ранг-апов:**
| Ранг | Фраза |
|------|-------|
| C | Пробуждение |
| CC | Сила крепнет |
| CCC | Воля закаляется |
| B | Путь воина |
| BB | Сталь и дух |
| BBB | Непреклонный |
| A | Мастерство |
| AA | Совершенство |
| AAA | Величие |
| S | Легенда |
| SS | Миф |
| SSS | Бессмертный |

**Механика ранг-апа:**
1. При выполнении карточки `mastery += adaptationMultiplier`
2. Когда `mastery >= masteryThreshold`:
   - Ранг повышается на ступень
   - `mastery` переносится остаток: `mastery -= masteryThreshold`
   - `masteryThreshold` увеличивается на 15%: `Math.round(threshold * 1.15)`
   - +1 очко в пул соответствующего атрибута
   - `escapeProgress += 1` (прогресс побега)
   - Запускается анимация ранг-апа
   - Через 2.2с открывается модалка **обязательного усложнения** карточки

---

### 4.3 Система адаптации

Адаптация — ключевой механизм против «застоя». Чем дольше карточка активна без усложнения, тем меньше XP и мастерства она даёт.

| Дней активна | Множитель | Эффект |
|-------------|-----------|--------|
| 0-6 дней | **100%** | Полная эффективность |
| 7-14 дней | **50%** | Половина XP и мастерства |
| 15+ дней | **10%** | Почти бесполезна |

**Сброс адаптации:** при редактировании карточки (изменение `meta` или `stat`) — `firstCompletedAt` и `daysActive` обнуляются, адаптация возвращается к 100%.

**Формула XP за выполнение:**
```
finalXp = round(15 * adaptationMult * (1 + (totalInt - 3) * 0.01))
```
Где `totalInt = STATS.int.value + gearBonuses.int`

**Формула мастерства за выполнение:**
```
masteryGain = adaptationMult (1.0 / 0.5 / 0.1)
```

---

### 4.4 Герой и атрибуты

**6 RPG-атрибутов:**

| Ключ | Название | Иконка | Эффект | Цвет |
|------|---------|--------|--------|------|
| `str` | Сила | ⚔ | Базовый урон по боссу | `#c73e4d` |
| `end` | Стойкость | 🛡 | Максимальное HP | `#60a5fa` |
| `int` | Интеллект | 🧠 | Множитель XP | `#c084fc` |
| `cha` | Харизма | 🎭 | Шанс крит. удара | `#fbbf24` |
| `wil` | Воля | 🧘 | Защита стрика при пропуске | `#34d399` |
| `agi` | Ловкость | ⚡ | (резерв) | `#fb923c` |

**Система пула атрибутов:**
- Каждый ранг-ап карточки даёт **+1 очко в пул** соответствующего стата
- При накоплении **5 очков** пула → **+1 к значению атрибута** (максимум 100)
- Это значит: **5 ранг-апов = +1 к стату**

**Формулы атрибутов в бою:**
| Атрибут | Формула |
|---------|---------|
| Базовый урон | `BASE_DAMAGE(5) + floor(sumAllStats * 0.5) + floor(gearStr / 5)` |
| Шанс крита | `min(50%, totalCha * 2%)` — крит удваивает урон |
| XP бонус | `1 + (totalInt - 3) * 1%` |
| Защита стрика | `totalWil / 300` — шанс сохранить стрик при пропуске |
| Макс. HP | `100 + (level - 1) * 5 + end.value` |

---

### 4.5 Уровни и XP

**Кривая XP (требуется для следующего уровня):**
```
Lvl 1:  100     Lvl 6:  4,000     Lvl 11: 128,000
Lvl 2:  250     Lvl 7:  8,000     Lvl 12: 256,000
Lvl 3:  500     Lvl 8:  16,000    Lvl 13: 512,000
Lvl 4:  1,000   Lvl 9:  32,000    Lvl 14: 1,024,000
Lvl 5:  2,000   Lvl 10: 64,000    Lvl 15: 2,048,000
```
После 15 уровня: удвоение каждый уровень.

**Эволюция имени героя:**
| Уровень | Имя | Титул |
|---------|-----|-------|
| 1-2 | Странник | «Тот, кто только начал путь» |
| 3-5 | Искатель | «Вспомнивший путь» |
| 6-9 | Воин | «Идущий сквозь тьму» |
| 10-14 | Страж | «Выстоявший в битвах» |
| 15+ | Архонт | «Повелитель судьбы» |

**Повышение уровня даёт:**
- +1 ко всем 6 атрибутам
- Макс. HP пересчитывается, HP полностью восстанавливается
- Бонусные артефакты: на Lvl 5 — Корона Архонта, на Lvl 10 — Плащ Теней
- Глобальная анимация, частицы, тряска экрана

---

### 4.6 Боссы подземелья

**3 босса, зависящих от прогресса побега:**

| Прогресс | Босс | Иконка | Особенность |
|----------|------|--------|-------------|
| 0-39 | 🐍 Змей Лени | 🐍 | Стандартный бой |
| 40-79 | 📱 Демон Соцсетей | 📱 | Двойной урон за пропуски |
| 80-140 | 🔥 Химера Выгорания | 🔥 | **Щит**: первые 5 ударов поглощаются |

**Каждый босс имеет 3 стадии:**
| Стадия | HP | Множитель урона | Описание |
|--------|-----|-----------------|----------|
| 1 | базовый | ×1.0 | «Я жду...» |
| 2 | ×1.5 | ×1.5 | Эволюция |
| 3 | ×2.0 | ×2.0 | Финальная ярость |

**HP боссов по типам:**

| Босс | Стадия 1 | Стадия 2 | Стадия 3 |
|------|---------|---------|---------|
| Змей | 100 | 150 | 200 |
| Демон | 120 | 180 | 250 |
| Химера | 150 | 220 | 300 |

**Механика боя:**
1. **Выполнение карточки** → расчёт урона → `changeBossHp(-dmg)`
2. **Крит** (шанс от Харизмы) → урон ×2, спецэффекты
3. **Состояние Hollow** → урон ×0.5
4. **Щит Химеры** → поглощает 5 ударов (каждый удар снимает 1 заряд)
5. **HP босса = 0** → переход на следующую стадию (с 3 → казнь)

**Казнь босса (`triggerBossExecution()`):**
- Глобальная анимация смерти (3 сек)
- 300 золотых + 200 красных частиц
- Тряска экрана (25 ед., 1.5 сек)
- Награды: **+1000 XP**, 2 случайных артефакта
- Запись в `_bossKills`
- Через 3 сек — оверлей «☠ ПОВЕРЖЕН»
- Через 8 сек — **возрождение нового босса** (с 1 стадии, полный HP)

**Визуальные эффекты боя:**
- Трещины (`bossCracks`) — opacity растёт от полученного урона
- Кровь (`bossBlood`) — opacity при HP < 30%
- `hue-rotate` на спрайте — визуальное искажение от урона
- Глаза следят за курсором (`mousemove` → `translate`)

**Наказание босса (23:00 МСК):**
- Каждый день в 23:00 МСК босс наносит урон за каждую **невыполненную карточку**
- Урон = `10 × stage.dmgMult` за карточку
- Эстус защищает от наказания
- Отображается обратный отсчёт в боковой панели

---

### 4.7 Цели

**3 типа целей с разными наградами:**

| Тип | Иконка | Дедлайн | XP | Урон боссу | Очков пула |
|-----|--------|---------|-----|-----------|------------|
| Краткая | 🟢 | 1-7 дней | +20 | -5 HP | +1 |
| Средняя | 🔵 | 1-4 недели | +50 | -12 HP | +2 |
| Долгая | 🟡 | 1+ месяц | +120 | -25 HP | +5 |

**Создание цели:**
- Выбор типа (визуальные карточки)
- Название, описание
- Дедлайн: **дата + время** (default 23:00)
- Количество шагов (default 3) + **текстовое описание каждого шага**
- Привязка к стату

**Текстовые шаги:**
- При изменении количества шагов в форме — автоматически появляются поля для описания каждого шага
- Каждый шаг имеет текст (например «Купить кроссовки», «Пробежать 3 км»)
- Если текст не введён — автогенерация «Шаг N»

**Выполнение цели (пошаговое):**
- Каждый шаг — кликабельная строка с чекбоксом (○ / ✓)
- Нажатие на шаг → `toggleGoalStep()`: отметка выполнения, +`xp / totalSteps / 2` XP, +1 очко пула стата
- Повторный клик → снятие отметки (отмена шага)
- **Авто-завершение**: при выполнении всех шагов цель закрывается автоматически
- Завершение: +полный XP, +очки пула, -HP боссу, частицы, тряска экрана

**Структура шага в объекте цели:**
```json
{
  "steps": [
    { "text": "Купить кроссовки", "done": true },
    { "text": "Пробежать 3 км", "done": false },
    { "text": "Пробежать 10 км", "done": false }
  ]
}
```

**Обратная совместимость:** старые цели (без массива `steps`) автоматически генерируют шаги вида «Шаг N» при отображении.

**Провал цели (`checkGoalDeadlines()`):**
- Проверка каждые **30 секунд**
- При истечении дедлайна:
  - Цель помечается `failed: true`
  - Удвоенный урон герою: `dmg * 2`
  - Тряска экрана, кровавый дождь
  - Toast «💀 Цель провалена!»

**Обратный отсчёт:** в карточке цели отображается время до дедлайна с цветовой индикацией:
- `safe` (зелёный) — > 24ч
- `warning` (жёлтый) — < 24ч
- `critical` (красный) — < 1ч или просрочено

---

### 4.8 Инвентарь и артефакты

**9 слотов экипировки (Paperdoll):**

| Слот | Ключ | Иконка (пусто) |
|------|------|----------------|
| Голова | `head` | ◇ |
| Амулет | `amulet` | △ |
| Торс | `chest` | □ |
| Плащ | `cape` | ◁ |
| Оружие | `weapon` | ✕ |
| Щит | `shield` | ◯ |
| Кольцо 1 | `ring1` | ○ |
| Кольцо 2 | `ring2` | ○ |
| Обувь | `boots` | ▽ |

**10 артефактов:**

| ID | Название | Ранг | Слот | Бонусы | Особое |
|----|---------|------|------|--------|--------|
| swordDiscipline | Меч Дисциплины | A | weapon | +5 ⚔, +2 🧘 | +10% урон по боссам |
| shieldWill | Щит Воли | A | shield | +6 🛡, +3 🧘 | Защита стрика +15% |
| amuletFocus | Амулет Фокуса | S | amulet | +8 🧠, +3 🧘 | +15% XP за привычки |
| ringCharisma | Кольцо Обаяния | B | ring1 | +5 🎭 | Шанс крита +5% |
| bootsWanderer | Сапоги Странника | B | boots | +5 ⚡, +2 🛡 | — |
| crownArchon | Корона Архонта | S | head | +3 ко всем | Все +3 |
| capeShadows | Плащ Теней | A | cape | +5 🧘, +3 ⚡ | Невидимость от искушений |
| chestVirtue | Кираса Доблести | A | chest | +8 🛡, +3 ⚔ | — |
| ringInsight | Кольцо Прозрения | A | ring2 | +5 🧠, +2 🎭 | — |

**Система лута:**
- **Шанс выпадения:** 8% (`LOOT_CHANCE`) при каждом выполнении карточки
- Не выпадают артефакты, уже находящиеся в рюкзаке или экипированные
- **Веса выпадения по рангам:**
  - S: вес 1
  - A: вес 3
  - B: вес 6
  - C: вес 10
- При убийстве босса — 2 гарантированных дропа

**Рюкзак:** 30 слотов максимум. Фильтрация: Все / Оружие / Броня / Аксессуары.

**Экипировка:**
- Клик по предмету в рюкзаке → панель свойств
- Кнопка «⚔ Экипировать» → надевает в слот
- Кольца: если ring1 занят, автоматически идёт в ring2
- При смене экипировки — пересчёт всех бонусов
- **Бонусы суммируются** и добавляются к базовым статам

**Функции:** `equipItem()`, `unequipItem()`, `discardItem()`, `getTotalGearBonuses()`

---

### 4.9 Карта подземелья

**14 комнат**, каждая открывается каждые 10 ранг-апов (`ROOMS_STEP = 10`):

| # | Название | Иконка | Ранг-апы | Лор |
|---|---------|--------|----------|-----|
| 1 | Камера заключенного | ⛓ | 0-9 | С чего начинается путь |
| 2 | Коридор Забытых | 🚪 | 10-19 | Те, кто забыл себя |
| 3 | Склеп Обетов | 💀 | 20-29 | Несдержанные обещания |
| 4 | Зал Разбитых Зеркал | 🪞 | 30-39 | Отражения лжи |
| 5 | Катакомбы Сомнений | 🕳 | 40-49 | Место страхов |
| 6 | Пещера Теней | 🌑 | 50-59 | Шёпот слабостей |
| 7 | Библиотека Рун | 📜 | 60-69 | Книги кровью |
| 8 | Тронный Зал | 👑 | 70-79 | Трон страха |
| 9 | Сад Забытых | 🌺 | 80-89 | Несбывшиеся мечты |
| 10 | Мост Раскаяния | 🌉 | 90-99 | Бездна ошибок |
| 11 | Башня Снов | 🗼 | 100-109 | Верхний мир |
| 12 | Кузница Воли | ⚒ | 110-119 | Закалка характера |
| 13 | Алтарь Истины | 🕯 | 120-129 | Горькая правда |
| 14 | Врата Свободы | 🌅 | 130-140 | Выход к свету |

**Визуальные элементы карты:**
- **Туман** (`#mapFog`): радиальный градиент, рассеивается от 20% до 80% по мере прогресса
- **Маркер героя** (🗡): парит над текущей комнатой с ripple-эффектом
- **Бейджи комнат:**
  - `▶ Сейчас` (золотой, пульсирующий) — текущая
  - `✓` (зелёный) — пройденная
  - `🔒 N` (серый) — закрытая, показывает сколько ранг-апов осталось
- **Прогресс-бар** внутри текущей комнаты
- **Звёздная анимация** фона (`starsDrift`)
- **Shimmer** на текущей комнате
- **Клик** на открытую комнату → модалка с деталями (иконка, лор, позиция, статус, % прогресса)

**Смена атмосферы:** при переходе в новую комнату меняется:
- `--atmosphere-hue` (цветовой тон)
- `--atmosphere-light` (яркость)
- `--mist-opacity` (плотность тумана)
- `--torch-intensity` (сила факелов)
- `--light-rays-opacity` (лучи света)
- Цвет частиц пыли (`dustParticles`)
- Цветовой фильтр на спрайтах босса

**14 уникальных тем (`ROOM_THEMES`):** каждая со своим набором hue/sat/light/mist/torch/rays/particleColor.

---

### 4.10 Система Hollow / Искупление

**Становление Полым:**
- Если HP героя падает до 0 от наказания босса
- Герой не умирает, а становится **Полым**
- Эффекты:
  - Титул: «Полый, потерявший цель»
  - Аватар получает класс `.hollow` (визуальное искажение)
  - **Урон по боссу -50%**
  - Появляется **полоса искупления** (3 сегмента)

**Искупление:**
- Нужно **3 идеальных дня подряд** (все карточки выполнены, 0 пропусков)
- Каждый идеальный день заполняет 1 сегмент
- При 3/3: возвращение человечности, HP = 50% от максимума
- Пропуск в любой день — счётчик обнуляется

---

### 4.11 Эстус

**Фляги Эстуса** — защита от наказания босса.

- **3 фляги** в месяц (восстанавливаются 1-го числа каждого месяца)
- 1 использование в день
- При выпивании: босс **не наносит урон** в эту ночь (23:00 МСК)
- Toast: «🧪 Эстус выпит»

---

## 5. Ежедневные циклы

### Часовой пояс
Все временные расчёты — по **Москве (UTC+3)**.

### Таймеры

| Интервал | Функция | Описание |
|----------|---------|----------|
| 60 сек | `checkDailyReset()` | Проверка смены дня (23:00 МСК) |
| 60 сек | `updatePunishCountdown()` | Обновление обратного отсчёта наказания |
| 30 сек | `checkGoalDeadlines()` | Проверка дедлайнов целей |

### `checkDailyReset()` — Ночная обработка

Срабатывает при смене дня (по МСК):

1. **Наказание босса:**
   - Подсчёт невыполненных карточек за прошедший день
   - Если есть невыполненные и не выпит Эстус:
     - `HERO.hp -= count × 10 × stage.dmgMult`
     - Кровавый дождь, тряска, спецэффект босса
     - Если HP ≤ 0 → состояние Hollow

2. **Подсчёт идеальных дней:**
   - `dailyCompletions > 0 && dailySkips === 0` → `consecutivePerfectDays++`
   - Иначе → сброс до 0

3. **Проверка искупления:**
   - Если Hollow и 3 идеальных дня подряд → восстановление

4. **Сброс дневных счётчиков:**
   - `dailyCompletions = 0`
   - `dailySkips = 0`
   - `estusUsedToday = false`
   - `chimeraShield = 5` (перезарядка щита Химеры)

5. **Месячный сброс Эстуса:**
   - При смене месяца: `estus = 3`

6. **Обновление `daysActive`** всех карточек

---

## 6. Статистика и достижения

### Вкладка «Статистика» (`renderStatsView()`)

**Карточки метрик:**
- Всего XP
- XP за 7 дней
- Среднее XP/день
- Всего выполнений карточек

**Графики:**
- **XP за 7 дней** — столбчатая диаграмма (flexbox bars)
- **Тепловая карта стиков** — 8 недель (56 ячеек), 5 градаций зелёного
- **Победы над боссами** — прогресс-бары по 3 боссам
- **Достижения** — сет карточек
- **Карточки по рангам** — счётчики каждого ранга

### 20 достижений (`buildAchievements()`)

| ID | Иконка | Название | Условие |
|----|--------|---------|---------|
| first_card | ⚔ | Первая ковка | 1 карточка |
| cards_10 | 📖 | Коллекционер | 10 карточек |
| cards_25 | 📚 | Архивариус | 25 карточек |
| rank_b | 🛡 | Воин | Ранг B+ |
| rank_a | ⚡ | Мастер | Ранг A+ |
| rank_s | 👑 | Легенда | Ранг S+ |
| lvl5 | 🗡 | Искатель | Уровень 5 |
| lvl10 | 🛡 | Страж | Уровень 10 |
| lvl15 | 🏰 | Архонт | Уровень 15 |
| xp_1k | ✨ | Первая тысяча | 1,000 XP |
| xp_10k | 💎 | Десять тысяч | 10,000 XP |
| xp_100k | 🌟 | Сто тысяч | 100,000 XP |
| goal_1 | 🎯 | Первая цель | 1 выполненная цель |
| goal_10 | 🏆 | Десятка | 10 выполненных целей |
| equip_all | 🎒 | Полный комплект | Все 9 слотов заняты |
| escape_half | 🗝 | Полпути | 70+ прогресс побега |
| escape_full | 🌅 | Свобода | 140 прогресс побега |
| streak_7 | 🔥 | Неделя дисциплины | 7-дневный стрик |
| streak_30 | 🔥 | Месяц железа | 30-дневный стрик |
| completions_100 | 💯 | Сотня | 100 выполнений |

---

## 7. Синхронизация данных

### 4 слоя хранения + многоуровневая защита

#### 7.1 Telegram CloudStorage (автоматически + вручную)

**Smart Cloud Sync** (`smartCloudSync()`):
- При запуске сравнивает timestamps облака (`meta.t`) и локали (`savedAt`)
- Если облако новее более чем на 10 сек — предлагает загрузку
- Если в облаке меньше карточек, чем локально — блокирует загрузку (защита от перезаписи)

**Автосохранение** (`autoCloudSave()`):
- Срабатывает при каждом `saveGameState()`
- Throttle: **30 секунд** (было 2 мин)
- **Блокировка:** если `FORGED.length === 0` — не сохраняет (защита от стирания)
- Чанкинг по **4096 символов** (`CLOUD_MAX_CHUNK`)
- Ключи: `nd_meta` (метаданные) + `nd_0`, `nd_1`, ... (чанки)

**Принудительное сохранение** (`forceCloudSave()`):
- Обходит throttle
- Используется при `beforeunload` и ручном сохранении

**Ручное сохранение** (`saveToCloud()`):
- Таймаут 10 сек (если облако не ответило — предложение использовать файл)

**Загрузка из облака** (`loadFromCloud()`):
- Чтение `nd_meta` → чтение N чанков → сборка JSON → `applySyncData()`

**Индикатор синхронизации** (`updateSyncBadge()`):
- ☁ зелёный — синхронизировано
- ☁ жёлтый — есть несохраненные изменения
- ☁ серый — нет связи с облаком

#### 7.2 IndexedDB (автоматически)

**Назначение:** Telegram WebView не очищает IndexedDB при сбросе кэша, в отличие от localStorage.

- `openIDB()` — открытие БД `neurodeck_db` при старте
- `saveToIDB(snapshot)` — запись снимка при каждом `saveGameState()`
- `loadFromIDB(callback)` — чтение для восстановления
- Проверяется при загрузке если localStorage пуст
- Проверяется в `deepRecovery()`

#### 7.3 Share Link

- `generateShareLink()`: `buildSyncData()` → JSON → `btoa(encodeURI())` → `#hash`
- Копирование в буфер обмена или `navigator.share()`
- `importFromHash()`: при загрузке страницы проверяет `location.hash`

#### 7.4 Файл `.ndsync`

- `downloadSyncFile()`: JSON → Blob → `.ndsync` файл
- `importSyncFile()`: FileReader → JSON parse → `applySyncData()`

#### 7.5 JSON Export

- `exportJson()`: отдельный экспорт для просмотра/бэкапа
- Файл: `neurodeck-export-YYYY-MM-DD.json`

#### 7.6 Глубокое восстановление (`deepRecovery()`)

Сканирует **все источники данных** и предлагает восстановить лучший вариант:

1. `localStorage['neurodeck_full_save']`
2. `localStorage['neurodeck_backup']`
3. `localStorage['neurodeck_cards_backup']` (третий ключ — обновляется только при FORGED > 0)
4. `localStorage['neurodeck_goals']` (legacy)
5. IndexedDB (`neurodeck_db`)
6. Telegram CloudStorage (`nd_meta` + `nd_N` чанки)

**Авто-восстановление:** при загрузке, если `FORGED.length === 0`, `deepRecovery()` запускается автоматически через 1 секунду.

#### `applySyncData(data, skipRender)`

Единая функция применения данных со всеми guard'ами:
- Санитизация HERO (HP/XP/level в допустимых пределах)
- Санитизация STATS (value 0..max, attributePoints ≥ 0)
- Санитизация FORGED (default rank/mastery/threshold)
- Защита от загрузки данных с меньшим числом карточек
- Полный ре-рендер всех UI

---

## 8. Атмосфера и визуал

### Слои подземелья (снизу вверх)

| Слой | Элемент | Описание |
|------|---------|----------|
| 1 | `.dungeon-bg` | Тёмный градиентный фон |
| 2 | `.light-rays` | Лучи света (CSS gradient) |
| 3 | `#mistLayer` | SVG-туман: 2 слоя `feTurbulence` с fractalNoise |
| 4 | `#dustCanvas` | Canvas: 60 частиц пыли, плавают вверх, цвет зависит от комнаты |
| 5 | `.torch-flicker` | Мерцание факелов (CSS animation) |
| 6 | `#particles` | Canvas: burst-эффекты (звёзды, искры, круги) |

**Параллакс тумана:** слои тумана двигаются в противоположных направлениях при движении мыши.

### Частицы (`Particle` class)

| Параметр | Default | Описание |
|----------|---------|----------|
| `vx/vy` | случайные | Скорость |
| `life` | 1.0 | Жизнь (0 = смерть) |
| `decay` | 0.015 | Скорость затухания |
| `size` | 3 | Размер |
| `color` | `#d4a574` | Цвет |
| `gravity` | 0.05 | Гравитация |
| `shape` | `circle` | `circle` / `spark` / `star` |

**Лимит:** 500 частиц максимум (старые удаляются при переполнении).

**Использование частиц:**
- Выполнение карточки: 28 золотых spark
- Крит: 40 золотых star + тряска
- Ранг-ап: 80 + 50 цветных
- SSS ранг: 120 золотых star
- Уровень: 100 золотых star
- Поражение босса: 300 золотых + 200 красных
- Экипировка: 40 цветных star
- Ковка: 60 цветных spark
- Цель: 120 цветных star

### `screenShake(intensity, duration)`

Тряска `.shakeWrap` через `requestAnimationFrame`:
- `translate(±intensity * progress, ±intensity * progress) rotate(±intensity * 0.3 * progress)`

### `spawnBloodRain(n)`

n капель крови с экрана (CSS `.blood-drop`), падают сверху.

### `pause` при `visibilitychange`

При сворачивании вкладки canvas-анимации останавливаются, при возврате — запускаются заново.

---

## 9. Аудио и тактильная отдача

### Web Audio API (`getAudioCtx()`)

Синтез 8-bit звуков через `OscillatorNode + GainNode`:

| Функция | Описание | Тоны |
|---------|---------|------|
| `sfxHit()` | Выполнение карточки | 200→100 Hz square |
| `sfxCrit()` | Крит. удар | 400, 600, 800 Hz |
| `sfxRankUp()` | Ранг-ап | 300, 450, 600, 900 Hz |
| `sfxLevelUp()` | Повышение уровня | 400→1000 Hz (6 тонов) |
| `sfxFail()` | Пропуск карточки | 300→100 Hz sawtooth |
| `sfxBossHit()` | Удар босса | 100→50 Hz sawtooth |
| `sfxForge()` | Ковка карточки | 150, 250, 400 Hz |
| `sfxGoalComplete()` | Завершение цели | 500, 650, 800, 1000 Hz |
| `sfxBossDefeated()` | Казнь босса | 200→1200 Hz (8 тонов) |
| `sfxEquip()` | Экипировка | 350, 500 Hz triangle |
| `sfxError()` | Ошибка | 150→80 Hz square |

AudioContext создаётся при первом клике пользователя (политика браузеров).

### Telegram HapticFeedback

| Тип | Когда |
|------|-------|
| `light` | Выполнение карточки |
| `medium` | Ранг-ап, ковка, экипировка |
| `heavy` | Крит, казнь босса, эволюция стадии |
| `success` | Уровень, завершение цели |
| `warning` | Повторное выполнение |
| `error` | Пропуск, провал цели |

---

## 10. Уведомления

### Browser Notification API

**Включение** (`toggleNotif()`):
- Запрос `Notification.requestPermission()`
- Сохранение в `localStorage['neurodeck_notif']`

**Расписание** (`scheduleNotifs()`):

| Триггер | Время | Текст |
|---------|-------|-------|
| Ежедневное | 22:00 МСК (за час до наказания) | «Осталось N карточек! Босс атакует через 1 час.» |
| Дедлайн цели | За 30 мин до истечения | «„N" — скоро истечёт дедлайн!» |

Уведомления планируются через `setTimeout` при загрузке (если в пределах 2 часов / 24 часов соответственно).

---

## 11. Онбординг

**8 шагов** для новых пользователей (если нет сохранения и онбординг не пройден):

| Шаг | Иконка | Тема |
|-----|--------|------|
| 1 | ⚔ | Добро пожаловать в NeuroDeck |
| 2 | 📖 | Колода карточек |
| 3 | 🔥 | Ранги и мастерство |
| 4 | 🐍 | Босс подземелья |
| 5 | 👤 | Герой и атрибуты |
| 6 | 🎯 | Цели |
| 7 | 🎒 | Инвентарь |
| 8 | 🚀 | Начни свой побег |

Показ с задержкой 1.2с после загрузки. После завершения: сохранение флага `neurodeck_onboarding_done`.

---

## 12. Жесты и навигация

### 6 вкладок

| Вкладка | Иконка | ID view |
|---------|--------|---------|
| Колода | 📖 | `view-deck` |
| Герой | 👤 | `view-hero` |
| Инвентарь | 🎒 | `view-inv` |
| Босс | 🐍 | `view-boss` |
| Карта | 🗺 | `view-map` |
| Статистика | 📊 | `view-stats` |

### Навигация

- **Табы** (верхние) + **нижняя панель** (`footer.bottom-nav`)
- **Свайп** влево/вправо по контенту — переключение вкладок (мин. 60px свайп)
- **Ctrl/Cmd+S** — открытие модалки синхронизации

### Event Delegation

Все клики обрабатываются через единый `document.addEventListener('click', ...)` с `data-action` атрибутами.

**Полный список `data-action`:**

| Action | Функция |
|--------|---------|
| `switch-view` | `switchView(view)` |
| `open-sync-modal` | `openSyncModal()` |
| `close-sync-modal` | `closeSyncModal()` |
| `save-cloud` | `saveToCloud()` |
| `load-cloud` | `loadFromCloud()` |
| `copy-share-link` | `copyShareLink()` |
| `share-link` | `shareLinkNative()` |
| `download-sync-file` | `downloadSyncFile()` |
| `choose-sync-file` | `syncFileInput.click()` |
| `export-json` | `exportJson()` |
| `reset-all-data` | `resetAllData()` |
| `toggle-notif` | `toggleNotif()` |
| `close-room-detail` | `closeRoomDetail()` |
| `open-forge` | `openForge()` |
| `close-forge` | `closeForge()` |
| `forge-card` | `forgeCard()` |
| `select-forge-stat` | выбор стата при ковке |
| `close-edit-card` | `closeEditCard()` |
| `select-edit-stat` | выбор стата при редактировании |
| `skip-edit-card` | `skipEditCard()` |
| `save-edit-card` | `saveEditCard()` |
| `open-goal-modal` | `openGoalModal()` |
| `close-goal-modal` | `closeGoalModal()` |
| `select-goal-type` | выбор типа цели |
| `select-goal-stat` | выбор стата цели |
| `create-goal` | `createGoal()` |
| `filter-goals` | фильтрация целей |
| `filter-backpack` | фильтрация рюкзака |
| `drink-estus` | `drinkEstus()` |
| `complete-card` | `completeCard()` |
| `fail-card` | `failCard()` |
| `edit-card` | `openEditCardDirect()` |
| `delete-card` | `deleteCard()` |
| `equip-item` | `equipItem()` |
| `unequip-item` | `unequipItem()` |
| `discard-item` | `discardItem()` |
| `advance-goal` | `advanceGoal()` (legacy) |
| `toggle-goal-step` | `toggleGoalStep(id, stepIdx)` |
| `complete-goal` | `completeGoal()` |
| `delete-goal` | `deleteGoal()` |
| `deep-recovery` | `deepRecovery()` |

---

## 13. Сохранение данных

### 4 слоя хранения

```
saveGameState()
  │
  ├── localStorage['neurodeck_full_save']     ← основной (мгновенно)
  ├── localStorage['neurodeck_backup']        ← резервный (мгновенно)
  ├── localStorage['neurodeck_cards_backup']  ← только при FORGED > 0
  ├── IndexedDB ('neurodeck_db')              ← не очищается Telegram
  ├── autoCloudSave() → CloudStorage          ← облако (throttle 30 сек)
  ├── saveGoals() → localStorage['neurodeck_goals']  ← legacy
  └── window.beforeunload → saveGameState + forceCloudSave
```

### 5 программных защит от потери данных

1. **Авто-восстановление в saveGameState()** — если `FORGED` пуст, восстанавливает из `neurodeck_cards_backup`
2. **neurodeck_cards_backup** — третий localStorage ключ, обновляется **только** при `FORGED.length > 0`
3. **autoCloudSave / forceCloudSave** — return если `FORGED.length === 0` (не перезаписывает облако пустым состоянием)
4. **smartCloudSync** — не загружает данные из облака, если в них меньше карточек, чем локально
5. **loadGameState** — проверяет emergency backup при пустом основном

### Авто-восстановление при загрузке

Если `FORGED.length === 0` после `loadGameState()`:
- `setTimeout(deepRecovery, 1000)` — глубокое сканирование всех 4 слоёв
- Диалог с предложением восстановить найденные данные

### Структура `saveGameState()` snapshot

```json
{
  "hero": { "name", "level", "xp", "hp", "isHollow", "estus", ... },
  "stats": { "str": {...}, "end": {...}, ... },
  "forged": [ { "id", "name", "rank", "mastery", ... } ],
  "goals": [ { "id", "type", "name", "deadline", "steps": [{"text","done"}], ... } ],
  "inventory": { "backpack": [...], "equipped": {...} },
  "escapeProgress": 0,
  "bossHp": 100,
  "bossStage": 0,
  "bossDefeated": false,
  "lastDayReset": "2026-06-15",
  "chimeraShield": 5,
  "forgedIdCounter": 100,
  "uidCounter": 10,
  "goalIdCounter": 1,
  "xpHistory": [ { "date": "2026-06-15", "xp": 120 } ],
  "bossKills": { "snake": 0, "social": 0, "chimera": 0 },
  "savedAt": 1718448000000
}
```

### Загрузка (`loadGameState()`)

1. Чтение `neurodeck_full_save`
2. Если нет → чтение `neurodeck_backup`
3. Если нет → чтение `neurodeck_cards_backup`
4. Если нет → `tryCloudRecovery()` (проверка облака)
5. Если нет → `loadFromIDB()` (проверка IndexedDB)
6. Если `FORGED.length === 0` → авто-запуск `deepRecovery()`

---

## 14. Схема всех взаимодействий

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          ИГРОК (ВЗАИМОДЕЙСТВИЯ)                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐    ✓execute     ┌──────────────┐    +XP     ┌────────┐│
│  │  КАРТОЧКА   │ ──────────────→ │    ГЕРОЙ     │ ─────────→ │ УРОВЕНЬ││
│  │  (привычка) │                 │   (атрибуты) │            │  UP    ││
│  └──────┬──────┘                 └──────┬───────┘            └────────┘│
│         │ ✕skip                          │ HP                          │
│         ↓                                ↓                              │
│  ┌──────────────┐              ┌─────────────────┐                      │
│  │  БОСС        │ ←─ damage ── │  НАКАЗАНИЕ      │                      │
│  │  (3 стадии)  │              │  (23:00 МСК)    │                      │
│  └──────┬───────┘              └─────────────────┘                      │
│         │ HP=0 (стадия 3)                                               │
│         ↓                                                                │
│  ┌──────────────┐              ┌─────────────────┐                      │
│  │  КАЗНЬ БОССА │ ── +1000 XP →│     ГЕРОЙ       │                      │
│  │  + 2 лута    │ ── loot ────→│  + _bossKills   │                      │
│  └──────┬───────┘              └─────────────────┘                      │
│         │ 8 сек спустя                                                  │
│         ↓                                                                │
│  ┌──────────────┐                                                      │
│  │  НОВЫЙ БОСС   │ ──→ цикл повторяется                                │
│  └──────────────┘                                                      │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    ПРОГРЕСС ПОБЕГА                                │   │
│  │                                                                   │   │
│  │  Ранг-ап карточки → escapeProgress +1                             │   │
│  │  Каждые 10 ранг-апов → новая комната на карте                     │   │
│  │  Прогресс 40 → смена босса (Змей → Демон)                        │   │
│  │  Прогресс 80 → смена босса (Демон → Химера)                      │   │
│  │  Прогресс 140 → Врата Свободы (финал)                             │   │
│  │                                                                   │   │
│  │  Смена комнаты → смена атмосферы (hue, mist, torch, particles)    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                      ЦЕЛИ                                         │   │
│  │                                                                   │   │
│  │  Создать цель (тип, стат, дедлайн, шаги)                          │   │
│  │    ↓                                                              │   │
│  │  + Шаг → +частичный XP, +1 пул стата                              │   │
│  │    ↓ (последний шаг)                                              │   │
│  │  Завершение → +полный XP, +пул стата, -HP боссу                  │   │
│  │                                                                   │   │
│  │  Дедлайн истёк → failed, -HP×2 герою                             │   │
│  │  Проверка каждые 30 сек                                           │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                  ИНВЕНТАРЬ / АРТЕФАКТЫ                            │   │
│  │                                                                   │   │
│  │  Шанс лута 8% при ✓ карточки                                     │   │
│  │    ↓                                                              │   │
│  │  Рюкзак (30 слотов) → Экипировать → 9 слотов                     │   │
│  │    ↓                                                              │   │
│  │  Бонусы к статам → пересчёт урона, крита, HP, XP бонуса          │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    HOLLOW / ИСКУПЛЕНИЕ                            │   │
│  │                                                                   │   │
│  │  HP = 0 от наказания → Полый                                      │   │
│  │  Полый: урон -50%, нужен UI-индикатор                             │   │
│  │  3 идеальных дня подряд → искупление                              │   │
│  │  Возврат: HP = 50% max, урон восстановлен                         │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                       ЭСТУС                                       │   │
│  │                                                                   │   │
│  │  3 фляги / месяц, 1 использование / день                          │   │
│  │  Выпить → босс не наносит урон сегодня ночью                     │   │
│  │  Восстановление 1-го числа месяца                                │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          СИНХРОНИЗАЦИЯ                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  saveGameState()                                                        │
│    ├── localStorage (x3: full_save + backup + cards_backup)  ← мгновенно│
│    ├── IndexedDB ('neurodeck_db')                            ← мгновенно │
│    ├── CloudStorage (auto, throttle 30 сек)                  ← фоново   │
│    └── beforeunload → saveGameState() + forceCloudSave()     ← при закр.│
│                                                                         │
│  Smart Sync при запуске:                                                │
│    └── smartCloudSync() → сравнение timestamps → предложение загрузки   │
│                                                                         │
│  Ручные методы:                                                         │
│    ├── ☁ Сохранить/Загрузить → CloudStorage (чанкинг 4096)             │
│    ├── 📋 Копировать → Share Link (#hash, base64)                      │
│    ├── ↗ Поделиться → navigator.share()                                │
│    ├── 💾 Скачать → .ndsync файл                                       │
│    ├── 📁 Загрузить → FileReader → JSON                                │
│    ├── 📋 JSON → экспорт для просмотра                                 │
│    └── ♻ Восстановить → deepRecovery() (4 слоя)                       │
│                                                                         │
│  Восстановление:                                                        │
│    ├── Авто: FORGED пуст → deepRecovery() через 1 сек                  │
│    └── Ручное: кнопка в окне синхронизации                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 15. Ключевые константы

| Константа | Значение | Описание |
|-----------|---------|----------|
| `ATTR_POOL_THRESHOLD` | 5 | Очков пула для +1 атрибута |
| `BASE_DAMAGE` | 5 | Базовый урон по боссу |
| `LOOT_CHANCE` | 0.08 (8%) | Шанс выпадения артефакта |
| `BOSS_HEAL_ON_FAIL` | 8 | HP босса при пропуске карточки |
| `ESCAPE_MAX` | 140 | Максимум прогресса побега |
| `ROOMS_STEP` | 10 | Ранг-апов на комнату |
| `CLOUD_MAX_CHUNK` | 4096 | Размер чанка CloudStorage |
| `CLOUD_META_KEY` | `nd_meta` | Ключ метаданных в облаке |
| `CLOUD_DATA_PREFIX` | `nd_` | Префикс чанков данных |
| `MSK_OFFSET_MS` | 3 часа | Смещение Москвы от UTC |
| `MAX_PARTICLES` | 500 | Лимит частиц на canvas |
| Auto-cloud throttle | 30000 (30 сек) | Мин. интервал авто-сохранения в облако |
| Dust particles | 60 | Кол-во частиц пыли |
| Heatmap weeks | 8 (56 ячеек) | Размер тепловой карты стиков |
| XP history limit | 90 дней | Глубина истории XP |

---

---

## 16. Performance режим (пункт 6 плана) — cache-bump v44 → v45

### NeuroDeck Performance Controller (`js/perf.js`)

Введена двух-ортогональная система режимов:

- **prefers-reduced-motion** — автоматически читается из ОС/браузера через `matchMedia`. При включении — отключаются все анимации/переходы через CSS-блок `@media (prefers-reduced-motion: reduce)`.
- **Performance Mode** (`localStorage["neurodeck_perf_mode"]`) — пользовательский переключатель. Значения: `auto` (по умолчанию), `low` (экономный), `effects-off` (без эффектов).

### CSS-блок `css/style.css`

Добавлен последним блоком: при `body.reduced-motion` / `body.low-effect` / `body.effects-off` отключаются анимации (`.shake-wrap`), упрощаются слои тумана и лучей света, скрывается dust-canvas. Сохраняется feedback `:focus-visible` для клавиатурной навигации.

### UI Toggle (syncModal)

В модальном окне Синхронизации добавлена секция "⚡ Режим производительности" с тремя радиочипами: 🟢 Авто / 🟡 Экономный / 🔴 Без эффектов. Состояние отображается в `#perfStatus`.

### Авто-детект

При загрузке проверяются `navigator.deviceMemory` и `navigator.hardwareConcurrency`. Если устройство слабое (mem < 4 или cores < 4) — при `mode=auto` показывается one-time toast с предложением включить Экономный режим.

### PixiJS Combat (`js/combat-pixi.js`)

При `NeuroDeckPerf.prefersReducedMotion()`:
  - `S.hitStop = 0` (нет заморозки)
  - `S.shake = 0` (нет screen-shake)
  - `S.flash` уменьшается на 60%
  - `spawnParticles(...)` пропускается при `isEffectsOff()`

Все манипуляции defensive: если `window.NeuroDeckPerf` отсутствует, хелперы возвращают `false` и не ломают боевой цикл.

### Audio / Haptic

`playTone()` (js/app.js) не модифицируется — сохраняем звуковую обратную связь. При желании можно расширить: `if (perf.mode === "low") return;` в начале каждой sfx-функции. Haptic остаётся включённым (важно для людей с проблемами слуха).

---

## 17. Telegram Bot Backend (пункт 7 плана) — в ветке feature/bot-backend

Backend для настоящих Telegram notifications реализован как отдельный сервис в подпапке `bot/`:
- `bot/index.js` — Express HTTP webhook (GET /api/health, POST /api/notify, POST /api/register-user)
- `bot/package.json` — node-telegram-bot-api, express, crypto (HMAC verify initData)
- `bot/Dockerfile` — node:22-slim
- `bot/.env.example` — TELEGRAM_BOT_TOKEN, SECRET_TOKEN, ALLOWED_ORIGIN
- `bot/README.md` — инструкция деплоя (Railway / Render / VPS)
- `bot/scheduler.js` — cron-таб: 21:30 МСК (напоминание за 1.5ч до наказания)
- `bot/db.json` — JSON-store чатов (in-memory на дев; persistent volume на проде)
- Тесты: `tests/bot-webhook.test.js`, `tests/notification-flow.test.js`

**Фронт интеграция (js/app.js + index.html):**
- `toggleNotif()` теперь имеет две ветки: Browser Notification API (было) И Telegram bot через `fetch(BOT_URL + "/api/notify")` (новое)
- Определяется через `USE_BACKEND_NOTIF` глобальную константу (задаётся при деплое)
- При недоступности bot — фронт автоматически падает обратно на Browser API
- Чекбокс в `index.html#syncModal`: "Использовать Telegram-уведомления через бот"

> ⚠️ **Деплой не сделан.** Ветка `feature/bot-backend` содержит код, который нельзя мержить в main без выбора хостера (Railway/Render/VPS/Cloudflare Workers). GitHub Pages не может запустить Node.js-сервер. После выбора хостера см. `bot/README.md` для шагов деплоя.

---

## 18. Audit (M7 из todo.md)

Прогнан автоматический audit через `node --check` всех JS-файлов, regex-поиск потенциальных проблем, проверка test coverage.

### Security audit
- ✅ `state-guards.js` покрывает все импорты через whitelist (card rank, artifact validation, counter clamping)
- ✅ HMAC initData верификация в bot backend (HMAC-SHA256 + bot token + secret_token middleware)
- ✅ Никаких `eval()`, `Function()` конструкторов в коде frontend / bot
- ✅ `esc()` используется во всех render-функциях для user-input strings
- ⚠️ **TODO:** при добавлении Telegram bot URL в config, проверить что он через HTTPS и rate-limited

### Performance audit
- ✅ `setInterval`/`setTimeout` — все очищаются через `clearInterval` при unload в новом коде; legacy таймеры в app.js не трогаются (они и так короткоживущие)
- ✅ PixiJS renderer инициализируется только при `#view-boss.active`
- ✅ При `prefers-reduced-motion` canvas-анимации не используют RAF (Pixi pause-not-documented, но hit-stop = 0 даёт визуальный эквивалент)
- ⚠️ **TODO:** явный `PIXI.Ticker.stop()` при уходе со вкладки босса (можно добавить через `visibilitychange`)

### Logic bugs
- ✅ `forgeCard()` стартовый ранг = C (locked)
- ✅ Apply sync data проходит через state-guards даже при corrupt JSON (sanitize() fallback)
- ✅ DailyReset (`checkDailyReset()`) запускается каждые 60сек, корректно срабатывает при смене MSK-дня
- ⚠️ **TODO:** race condition между `saveGameState()` и `autoCloudSave()` — обе асинхронны; при двойном сохранении Throttle = 30s, поэтому риск низкий

### UX audit
- ✅ touch targets: кнопки в app.js ≥ 36px (passing iOS HIG ≥44px on most)
- ⚠️ **TODO:** long-press на мобильных иногда ломает scroll на Firefox Android — добавить `touch-action: manipulation` в CSS
- ✅ Cache-bump версии синхронизированы (все `?v=45`)
- ✅ Все emoji отображаются на уровне ОС (Georgia serif fallback)

### Documentation
Добавлен раздел §16 (Performance) и §17 (Bot Backend) — этот changelog.

*Документация частично устарела (базовая версия `?v=23`, коммит `7e44765`). Актуальная спецификация баланса v2 и техдолга — в разделе «Обновление 2026-08-25» выше и в скиллах `.opencode/skills/neurodeck-*`.*

# NeuroDeck — Handoff сессии 2026-09-15 → следующая

## Статус: «Твердыни v2» — UX-полировка + фиксы + фичи применены

Коммиты сессии (main, pushed): `97b4919` Ф1 · `8776c57` Ф2 · `039168c` Ф3 · `452581f` Ф4 · `edfe17b` Ф5
Кэш: v59 · Схема: v9 · Предыдущий хандофф: docs/session-handoff/SESSION-2026-09-14.md

---

## Что сделано за сессию

### Ф1 — Быстрые фиксы + QA-реставрация (97b4919)
- **404 спрайтов**: `img/units/t1.png → tier1.png` в ДВУХ местах (аватар героя app.js:1421 и иконки найма :1879) — 24×404 → 0.
- **Легаси-строки**: HP-тост на level-up (там +30💰), desc Стойкости «HP»→«Оборона», «твердыен», «Sender-Хутор», «11 локаций» (легаси Тракта) → переписаны; ADR-0001 опечатка; + «доход тракта» в notification (Ф4).
- **Квест-доска**: реальные счётчики прогресса (карточки/золото/найм/стройка/задача/штурм), самозабор закрыт до выполнения цели, прогресс персистится (`dailyQuests.progress`), e2e-тест.
- **Босс-JPG удалены** (ортфаны вырезанной боёвки), пометка в ART_MANIFEST §5.
- **6 QA-шагов починены (все — тест-баги, продукт чист)**:
  - `builtAt`-иммунность (7 дней) блокировала распад в сценариях; фикс — бэкдейт builtAt в тесте.
  - Случайное событие «Караван» (+20💰) рандомно оплачивало содержание 21 → срыв детерминированного сценария руины (P(провал)≈67%); фикс — пин Math.random на блок 4.
  - `assaultUI` сбрасывал несуществующий `assaultUsedDay` вместо `siege.assaultDay` → детерминированный таймаут на 2-м штурме.
  - 0.3/1.3: ожидания под устаревший SPEC (sh01 total 15); канон — stronghold-data (total 5, sim-валидирован). 1.3 attrition теперь считается через SM.assaultOutcome.
  - 10.2: байт-сравнение заменено на стабильное (санитайзер канонизирует порядок ключей; `builtAt: null` нормализуется) — контент-инвариант сохранён.

### Ф2 — A2+A4+A3 (8776c57)
- **Ранг-ап**: баннер 2.2с→1с, эволюция встроена в editCardModal (чипы путей, применяется на «Сохранить»), evolutionModal удалён; 3 интеграционных теста переписаны под новую архитектуру (гарантии те же).
- **Панель твердыни**: «Что построить сейчас» = топ-3 дешёвых доступных, полный каталог по кнопке (сброс при открытии другой твердыни).
- **Типографика**: `--fs-secondary: 13px` вместо 10px (57 мест), тесные бейджи оставлены мелкими; на coarse-pointer `.card-btn` 44×44.
- Кэш v57→v58, visual-снапшоты перепинованы.

### Ф3 — A1 Онбординг (039168c)
- 8 экранов-лекций → 1 welcome-карточка (3 практические строки) + `hintOnce`-подсказки при первом касании систем (вкладки, первая карточка, первая панель твердыни). Тот же localStorage-флаг — e2e не тронуты.

### Ф4 — B1+B2 (452581f)
- **Воскресный дайджест**: нормализованный бар-чарт недели (значения + подсветка сегодня), 4 плитки, прогресс кампании N/20 + номер осады.
- **Галерея трофеев**: у каждого достижения cur/max → прогресс-бары; разблокированные с золотым свечением, шапка с общим прогрессом.

### Ф5 — B4+B3 (edfe17b)
- **Trade routes**: путь = пара соседних захваченных твердынь; +2% налогов/путь, кап +38%. Формулы в stronghold-model (tradeRoutes/tradeBonus), ЕДИНАЯ реализация в app (тик + дисплей) и симе. Сим: **ALL GATES PASS**, средний темп 42→34 нед (эндгейм; ранняя игра не тронута). BALANCE.md круг 6.
- **Сезоны**: схема **v9** (миграция сеет Сезон 1 со снапшотом прогресса), 30 дней, чип с обратным отсчётом и прогресс-баром во вкладке Твердыни, финал в checkDailyReset с модалкой дельт (XP/выполнения/твердыни/казна), цикл имён (Пробуждение→Закалка→Разлив→Венец), sanitizeSeason, персист полный.

---

## Тесты (последний полный ci — ЗЕЛЁНЫЙ, exit 0)

| Сюита | Было (базлайн утра) | Стало |
|---|---|---|
| unit | 177/177 | **183/183** (+trade-routes 3, +season 3) |
| e2e logic+app | 14/14 | **15/15** (+квест-счётчики) |
| visual | 2/2 | 2/2 (снапшоты v58) |
| strongholds playtest | 17/18 | **18/18** |
| acceptance | 32–34/40 (флак) | **40/40** |
| Консольные 404 | 24 | **0** |
| Сим | ALL GATES | ALL GATES (круг 6) |

Замечание: e2e раз в ~5 прогонов ловит инфра-флак «browser has been closed» (не ассерт) — перепрогон лечит.

## Известные ограничения (актуализировано)

1. События дня: Караван/Тихий день работают; Кузнец (скидка найма −25%), Рынок (налоги ×1.5 сегодня), Духи дремлют — **объявление показывается, эффекта нет** (заглушки).
2. «Сила духа подземелья»: легаси-цитаты грепом не найдены (HP/Эстус/осколки вычищены) — пункт считать закрытым, финальная сверка цитат при случае.
3. Оффлайн-докрутка кап 7 дней — by design.
4. СПЕК.md таблица sh01 (gar 10/def 5/total 15) расходится с данными (3/2/5) — данные каноничны (sim), SPEC-таблицу обновить при следующей ревизии доков.
5. e2e инфра-флак «browser has been closed» (~1/5 прогонов) — chromium/ресурсы, не продукт.

## Осталось (кандидаты на следующую сессию)

1. **Плейтест живым игроком** — всё ещё главный гейт: пройти неделю на телефоне (t.me/NeuroDeckBot/neurodeck, v59).
2. Дожить события дня до логики (Кузнец/Рынок/Призраки) — цены/эффекты уже описаны в объявлениях.
3. Соцкрючок «общий фронт» (нужен бэкенд) — по-прежнему отложено.
4. Гарнизонный резерв в sim.js (приоритет C из прошлого хандоффа) — не трогали.
5. Опционально: детали галереи трофеев (редкости/награды за 100%).

## Инфраструктура — без изменений
GitHub Pages auto-deploy · бот systemd (xray SOCKS5) · 6 агентов студии · edit-protocol в силе (v59 пины править при бампе: index.html + tests/perf.test.js + tests/strongholds-ui.test.js).

---

# Раунд 2 (та же дата, продолжение сессии)

## Коммиты: 0239b9b → 665fee1 → 7b307e7

## Что сделано

### Пак 1 — Экономика (0239b9b)
- Призраки: **−1💰 за каждую ночь существования** (было однократно при просрочке), кап 5💰/ночь — анти-спираль. Гнев считается от НОВЫХ призраков недели.
- Все 5 событий дня РАБОТАЮТ (были заглушки): Кузнец ×0.75 найм, Ярмарка ×1.5 налоги, Духи дремлют = ночь без списаний. Ролл перенесён ДО тиков (событие действует в свой день), offline-ночи событийно нейтральны. `dailyEvent` персистится (v9-сейв).
- SPEC дополнение + BALANCE круг 7. Сим призрачных списаний не моделирует — G1–G3 не затронуты.

### Пак 2 — Инфра (665fee1)
- telegram-web-app.js → async + `window.__tgReady()` (boot + onload + window.load); Google Fonts → media="print" swap + noscript. Висящая сеть больше не блокирует бут (probe: DCL 423мс при вечном ханге).
- Бот передеплоен в /opt (дрейф закрыт), сервис перезапущен (одобрено юзером), active.

### QA Раунд 3 — 6 зон параллельно (485f57f)
Студия `.opencode/agent/qa-{visual,data,economy,chaos,infra,lead}.md` + харнесы playtest-data/chaos/qa-economy-parity (в ci). Находки и фиксы: P1 goalModal-стекинг, P1 рынок-доход (parity с sim.js:429 восстановлен), P1 768px-breakpoint 760→900, P2 экспорт/импорт-вайп (exportJson = buildSyncData), P2 anyFell TDZ, P3 newGame-хедер/вайп-сезон.

### QA Раунд 4 — мульти-вкладка и гонка облака (7b307e7)
- LWW-гвард: поколение `neurodeck_gen` — чужая свежая запись адоптируется перед сборкой снапшота; storage-event живой синк между вкладками (debounce 400мс).
- Гонка облака: `localEpoch` — отложенные applySyncData из smartCloudSync отбрасываются при локальных изменениях во время полёта/диалога (+ forceCloudSave навстречу).
- playtest-data 20/20 (B12 переписан, +B17 гонка со стабом CloudStorage).

### Мелочи
- card-rank сдвинут (right:64) — перекрывал 🗑 на всех карточках; бейдж Клятвы → верхний левый угол; O-10 закрыт (ever_saved только при FORGED>0, вайп больше не пересохраняет); forge ≤40 симв; acceptance 4.1/8.1 гасят событие дня в фикстурах.

## Текущее состояние
Полный ci зелёный: unit 184 · e2e 16 · visual 5 · strongholds 18/18 · acceptance 40/40 · data 20/20 · chaos 31/31 · parity 215/215.

## Решения совета (зафиксированы)
- **sh01 (незахваченный стартовый лагерь) освобождён от содержания** — реализуется в этом раунде.
- **Эндгейм-ядро**: сезонные награды (+2% налогов/сезон, стак до +10%, схема v10) + «Вечный трон» (sh20: 100k💰 → +1% налогов навсегда, ×5) + BALANCE круг 8. Элитные юниты Т8+ — отдельный следующий круг.
- **D-приоритет следующей сессии: гарнизонный резерв в sim.js.**
- Соцкрючок «общий фронт» (бэкенд) — в бэклоге после резерва.

## Открытые хвосты
- Призраки-семантика закрыта (ночные списания); остались: мульти-вкладка LWW и гонка облака — ЗАКРЫТЫ раундом 4 (см. выше).
- visual-P3: V-6..V-11 (фиксируются текущим раундом), тосты-headless (оговорка).
- SPEC-таблица sh01 (10/5/15) расходится с данными (3/2/5) — синхронизируется текущим раундом.
- Бот: restart counter=5 — наблюдать.
- Эндгейм: Т8+ юниты — следующий круг баланса.

## Раунд 5 (финал сессии): полировка V-6..V-11 + эндгейм-ядро
- Коммиты fba7ae1 (полировка + sh01) и a9b422d (эндгейм v10).
- Полировка: V-6 fade 0.25с · V-7 scroll-top вкладок · V-8 дайджест 7 колонок (будущие призрачные) · V-9 nbsp · V-10 пилюля Синхры с иконкой · V-11 тосты/дух над navbar+safe-area · sh01 до захвата без содержания/коррапшна · SPEC sh01 = 3/2/5 (канон — stronghold-data.js).
- Эндгейм (ENDGAME.md, BALANCE круг 8, схема v10): венцы сезонов +2% налогов (стак до +10%), Вечный трон sh20 100k💰/ярус (+1% навсегда, ×5), taxMultiplier() единый для тика/дисплея. Т8+ юниты — следующий круг.
- v10-пины: playtest-data (v→10 в 8 шагах), acceptance 9.1/9.2 (v 9→10), storage-v8 тест, season-тест +crownBonus.
- Следующая сессия: **гарнизонный резерв в sim.js** (D-приоритет), далее Т8+ и соцкрючок. Живой плейтест юзера — продолжается.

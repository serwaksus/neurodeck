# NeuroDeck — Полный аудит приложения

**Дата:** 2026-09-17 · **Метод:** 6 субагентов-тестировщиков с перекрёстной связью через общую доску (2 раунда: независимые зоны → ответы на межзонные вопросы) · **Базлайн:** `npm run check:js` OK, юнит-сюиты 184/184 [F]

## Зоны

| ID | Зона | Объект |
|----|------|--------|
| T1 | Состояние/хранилище | storage.js, state-guards.js, миграции v8→v10, multi-tab, облако |
| T2 | Игровая логика/экономика | app.js: тик, события, призраки, трейд-роуты, сезоны, эндгейм |
| T3 | Визуал/статика | index.html, css, img/, разметка, XSS, типографика |
| T4 | Рантайм-гейты V+F | e2e, visual, 5 сим-харнессов, флак ×5, визуальный обход |
| T5 | Legacy/perf | миграции, localStorage edge, perf-режимы, утечки |
| T6 | Доки/консистентность | PROJECT_DOCUMENTATION, SPEC, CI, AGENTS.md, хандоффы |

Артефакты: `/tmp/opencode/neurodeck-audit/board/` (FINDINGS.md, QUESTIONS.md, 18 скринов, репро-скрипты), репро T1 `/tmp/opencode/t1-repro-staged.js`, `t1-repro-E.js`, probe T5 `/tmp/opencode/t5-migration-probe.js`, логи T4 `/tmp/opencode/t4-*.log`.

---

## 1. Обязательные гейты (верифицированы T4, рантайм)

**GATE-V (визуал): PASS.** 6 реальных вкладок (Колода/Квесты/Герой/Инвентарь/Твердыни/Статистика) × 2 вьюпорта (390×844, 768×1024), starter-модалка, онбординг; **0 console.error / 0 pageerror / 0 requestfailed**; visual-регресс 5/5. Скрины: board/screens/ (18 PNG).

**GATE-F (функциональность): PASS.** Честные клики (не force): навигация, sync/task-модалки, онбординг; live-sync A→B работает. Рантайм-подтверждён дефект T1-H3 (см. ниже).

**Наборы тестов:** e2e 16/16, visual 5/5, strongholds 18/18, acceptance 40/40, qa:data 20/20, qa:chaos 31/31, qa:parity 215/215 — все OK [F]. Флак e2e: 1 падение из 5 прогонов (`browser has been closed`, инфра; перепрогон зелёный) → для CI желателен retry.

**CRITICAL-находок нет.** Но два HIGH по эффекту на игрока граничат с критичностью (T2-H1 — краш вкладки на эндгейме; T1-H4 — необратимая потеря трона при переносе).

---

## 2. Итоги по severity

**HIGH: 6 · MEDIUM: 22 · LOW: 20 · INFO/PASS-подтверждений: 25+**

### HIGH (все подтверждены репро/рантаймом, 4 из 6 — перекрёстно вторым агентом)

| ID | Суть | Evidence | Перекрёстно |
|----|------|----------|-------------|
| T1-H1 | Миграция v7→v8 без каталога StrongholdData молча стирает прогресс крепостей (synthetic `sh1..sh20` → sanitize по каталогу `sh01..sh20` отбрасывает; далее пустой массив уезжает в облако) | repro-стейдж B: captured 10→1; storage.js:76-87, state-guards.js:257-276 | T5-C3: подтверждено + окно восстановления шире (счётчик живёт в `season.snapshot.captured`) |
| T1-H2 | Stale-write guard мёртв: `stateGen` нигде не инкрементируется → multi-tab = слепой LWW, adopt-ветка недостижима (заявлен починенным в 7b307e7 — не так) | grep: 0 инкрементов; storage.js:32,175,794 | T5-C1 (риск оценён MEDIUM: смягчает live-sync 400ms); T4-L1: транспорт жив, мёртв именно guard |
| T1-H3 | Cloud-save на выходе молча пропускается всегда при наличии meta в облаке (`savedAt` отсутствует в buildSyncData → конфликт-чек всегда истинен) → облако устаревает после каждой сессии | repro E4: 0 setItem; storage.js:232,237; app.js:2920 | **T4-M2: подтверждено в реальном браузере** (`page.close({runBeforeUnload:true})` → 0 setItem, 5/5) |
| T1-H4 | `buildSyncData` (storage.js:558-574) не переносит `throne` и `dailyEvent` → любой перенос (облако/файл/ссылка) сбрасывает Вечный трон в 0 **необратимо** (локальный сейв перезаписывается нулём) | repro E1/E3; T2 усилил: переезжает и `lastDayReset` → тик не идёт, событие дня null до полуночи (−1/5 дней Ярмарки ≈ −5.7k💰 в поздней игре [C]) | T5-C2 подтверждено; T6-I3: SPEC формально не нарушен, фактически — потеря |
| T1-H5 | Prototype pollution: own-ключ `__proto__` из JSON импорта (share-ссылка/файл/облако) проходит `if (!STATS[k]) return` и пишет в `Object.prototype` | repro-стейдж A: `({}).evil=1`; storage.js:800-810 | — |
| T2-H1 | Precedence-баг app.js:1897: `'+ STRONGHOLDS[fi] ? … : …'` — `+` сильнее `?:`. При fi=-1 (все 20 захвачены) → `STRONGHOLDS[-1].name` → **TypeError, вкладка твердынь падает ровно на эндгейме**; при валидном fi строка «Фронт/Осада/Гнев» никогда не рендерится | node-репро: THROW + голое имя | Fix одной строкой: скобки вокруг тернарника |

### MEDIUM — топ по риску (полный список в board/FINDINGS.md)

| ID | Суть |
|----|------|
| T5-M1 | Quota exceeded на первом `setItem` (storage.js:208) обрывает ВСЮ цепочку: backup, cards_backup, **IndexedDB и облако не записываются** — резервные пути мертвы ровно когда нужны |
| T5-M2 | Полностью недоступный localStorage (Safari «блокировать cookie», приват-режим) → top-level throw, приложение не стартует: пустой экран без recovery (app.js:2921,3317; storage.js:393) |
| T1-M3 + T5-C4 | Переполнение 200 чанков облака: тихий abort без onDone → `_pushInFlight` висит вечно → автооблако мертво до перезагрузки; сейвы >~800КБ не попадают в облако никогда |
| T2-M1 | Золото целей — фикция: тост «−N 💰» при провале не списывает, `completeGoal` не платит `goal.gold` (grep всех мутаций HERO.gold [F]) |
| T2-M2 | Квест «Заработай 30💰» считает только доход твердынь (Караван/задачи/квесты мимо) + backfill до 7 тиков капает в сегодняшний прогресс |
| T2-M3 | Даты задач не санитайзятся: битый `ghostSince` → NaN → призрак/сундук зависают навсегда, UI «ещё 0 дней» |
| T5-M3 | screenShake (rAF + inline transform) не гейтится eco/reduced-motion — вопреки декларации perf.js:6-8; вестибулярный a11y-риск, 20+ call-site'ов |
| T5-M4 | Битый v10-сейв (season стёрт) хилится с потерей венцов: crownBonus сброшен в 0 без предупреждения (компенсации нет) |
| T1-M1 | Удаление последней карточки невозможно: любая дата-save молча воскрешает её из `neurodeck_cards_backup` |
| T1-M2 | Задачи без числового id коллапсируют в одну (sanitizeCounter даёт всем id=1) |
| T1-M4 | Цели из `neurodeck_goals` — единственная сущность без санитайза при загрузке (app.js:1050) |
| T4-M1 | Штатный e2e таб-навигации кликает `force:true` (neurodeck.test.js:46) — блокирующий оверлей сделал бы тест «зелёным» при неработающих табах; + флак ~20%/прогон |
| T3-M1 | Чузер целей обещает «−5/−12/−25 HP» (index.html:612/622/632) — HP-механики не существует (GOAL_REWARDS = xp/gold/statXp); статика дезорирует |
| T3-M2 | img/bosses/: 9 мёртвых jpg (0 ссылок в репо) при манифесте «удалены 2026-09-15» — диск vs ART_MANIFEST §5 |
| T6-M1 | PROJECT_DOCUMENTATION §13 («актуальная») описывает souls-схему v5; фактический снапшот v10 — 8 крепостных полей, ни одно не задокументировано; миграционный тулинг по докам писать нельзя |
| T6-M6 | GitHub CI гоняет 4 из 9 команд `npm run ci` (ci.yml:20-27): плейтесты и qa-data/chaos/parity в workflow отсутствуют — «полный ci зелёный» ≠ GitHub CI зелёный |
| T6-M3 | SPEC §9: `army/siege` схема расходится с фактической миграцией (`week` vs `weekCount`, `frontSince` не существует) |
| T6-M4 | SPEC §8 «Σ max 7322» vs канон §1 и stronghold-data «Σ 8352» (пересчитано [C]: 247+85+8020; пин сима qa-economy-parity.cjs:135) — чинить §8 |
| T6-M5 | AGENTS.md: «30 skills» — на диске 12; `shader-dev` не существует, `e2e-testing-patterns` не задокументирован |
| T6-M2 | §15: 5 из 13 «ключевых констант» не существуют (BASE_DAMAGE, LOOT_CHANCE, …) |
| T6-M7 | §4.1 «стартовый ранг можно задать любой» — код жёстко `C` (app.js ~1278) |
| — | T2-I1/T6-I3: **заявленные заглушки Кузнец/Рынок/Духи НЕ подтвердились — все три события работают** (app.js:1582, 1441-1442, 2154); список ограничений хандоффа устарел (T6-L2) |

### LOW — сводка

Арифметика `daysToSiege` не до понедельника (T2-L1, сейчас не рендерится из-за T2-H1); скачок налогов sh12→sh13 ×5.2 — design-wall, законтролен пином сима (T2-L2); rollback часов продлевает вчерашнее событие (T2-L3); `meta.n` не клампится в loadCloudChunks (T1-L1); один try на 7 полей снапшота с misleading-тостом (T1-L2); повторная миграция при битом `v` теряет гарнизоны (T1-L3); зомби-прогресс после new game только в keep-cards через квота-фол — окно узкое (T2-R2); live-sync рендерит не всё (инвентарь второй вкладки, T5-L1); perf-compat: мёртвый PIXI-мост (T5-L2); FPS-автодетекта нет (T5-L3); 10 иконочных кнопок «✕» без aria-label (T3-L3); 10px-остаток .ach-prog-text (T3-L4); кэш v60→v61 в a084b20 без упоминания (T3-L5); внешний SPOF image.qwenlm.ai — деградация мягкая (T3-L6); статичный «+100 HP» pre-JS вспышка (T3-L7); PROJECT_DOCUMENTATION кластерно устарел: ?v=23 vs v61, 6 вкладок vs фактические, §16 PixiJS удалён, §17 бот-файлы не существуют, §18 HMAC-верификации нет (T6-L1); check:js покрывает 4 из 7 js (T6-L3); пины хандоффа «30→6500/1→10 дней» противоречат каталогу 60..10800 (T6-L4); trade routes отсутствуют в SPEC (T6-L5).

---

## 3. Перекрёстная связь агентов (2 раунд — ответы)

| Вопрос | Ответ |
|--------|-------|
| T1→T2 «зомби-прогресс после new game» | Полный сброс чист [F]; зомби только в keep-cards при квота-фоле (IDB 'latest' переживает стёртый localStorage) — LOW, окно узкое |
| T1→T4 «e2e-гэпы multi-tab/beforeunload» | Подтверждены: beforeunload-гэп воспроизведён рантаймом; для multi-tab нужен конфликтный сценарий — ожидаемый ассерт: adopt никогда не выполняется (T1-H2), побеждает слепой LWW; стаб CloudStorage готов (board/probe.cjs) |
| T1→T5 «v7-миграция всегда при живом каталоге?» | Да: sync-порядок скриптов index.html:1333-1337 ставит StrongholdData до loadGameState; probe: проход v6→10 идемпотентен; потеря — на следующем sanitize, окно восстановления шире (счётчик в season.snapshot.captured) |
| T1→T6 «SPEC §sync vs факт» | Раздела о синхронизации в SPEC нет; из двух гардо́в 7b307e7 работает только localEpoch, LWW мёртв — чинить код, доки после |
| T1→T2 «dailyEvent при переносе» | Подтверждено и усилено: переезжает lastDayReset → тик не идёт → событие null до полуночи; потеря до ~+5.7k💰 1/5 дней [C] |
| T6→T2 «канон Σ налогов» | 8352 — канон (пин сима qa-economy-parity.cjs:135; данные stronghold-data.js) → править SPEC §8 |
| T6→T1 «фикс T1-H4 одним патчем» | Принят: `throne` + `dailyEvent` добавлять в buildSyncData одним патчем |
| T6→T4 «пины vs v61» | Все сюиты на HEAD (v61) зелёные: e2e 16/16, visual 5/5 — пины согласованы |
| T3→T4 «внешний фон + 27 спрайтов» | 0 requestfailed на всех страницах; onerror-фолбэк спрайтов; GATE-V PASS покрывает |
| T3→T1 «hp-поля в легаси» | MIGRATIONS[7] удаляет hp/maxHp/isHollow (probe T5); 0 «hp» в app.js — пути утечки в UI нет |
| T3→T6 «9 босс-jpg: удалить или вернуть в манифест» | Рекомендация: удалить файлы (0 ссылок), манифест дополнить строкой — решение за владельцем |
| T5→T1 «порядок записи при quota» | Рекомендация: переставить порядок (backup/IDB/cloud ДО full_save) + per-key try — закрыть T5-M1 и T1-L2 вместе |
| T5→T3 «гейт screenShake» | Рекомендация: гейт внутри функции screenShake (1 строка, покрывает все 27+ call-site'ов) |
| T5→T2 «компенсация сброса crownBonus» | Механики компенсации нет — потеря перманентного бонуса до конца сезона; порог частичной порчи, отметить вKnown-issues |

---

## 4. Чисто (PASS-подтверждения)

- **Экономика формульно едина:** app.js не имеет собственных копий — только SM.*; parity-сим сверяет 19 путей, кап +38%, пин 11526💰 [F] (T2-I2).
- **Границы циклов корректны:** сезон 30→31-й, backfill кап 7, MSK-понедельник, siege.week кап 520, W_eff кап 12; «кампания 20 недель» в коде не существует — кампания = 20 твердынь + трон 5 ярусов (T2-I3).
- **NaN/деление на ноль:** чисто — assaultOutcome/corruptionTick/levelUp/tradeBonus все пути покрыты (T2-I5).
- **Ghost drain = min(5, призраки)/ночь** ✓ (T2-I4).
- **XSS-векторов нет:** esc() покрывает все пользовательские строки в innerHTML; showToast через textContent (T3).
- **Пути картинок 27/27**, «эстус»/«осколок» 0, разметка валидна (0 незакрытых, 0 дублей id из 118), зум не отрублен, .card-btn 44×44 на coarse (T3).
- **Все 19 JSON.parse в storage.js обёрнуты в try/catch** (T1-I1, подтверждено T5).
- **Миграции идемпотентны в реальном flow** (probe: v6→10 корректен, повторный = no-op) (T5-I1).
- **Eco-режим: rAF-циклы останавливаются корректно, утечек rAF/interval/listener не найдено** (96 matches сверены) (T5-I2).
- **SPEC ↔ stronghold-data.js: полная сверка без расхождений** — 20 твердынь × 5 полей, 20 построек, 7 тиров, формулы §4-§6 (T6-I1).

---

## 5. План исправлений (приоритизирован)

**P0 — потери данных/краш (1 спринт-час + тесты):**
1. T2-H1: скобки вокруг тернарника app.js:1897 (1 строка) + регресс-тест «20/20 захвачено → вкладка рендерится».
2. T1-H4(+T6): добавить `throne` и `dailyEvent` в buildSyncData (1 патч) + тест переноса.
3. T1-H3: убрать `savedAt || 0`-ловушку — писать savedAt в buildSyncData или скорректировать конфликт-чек (не ломая анти-откат) + e2e-ассерт из T4-протокола.
4. T1-H5: `Object.prototype.hasOwnProperty.call(STATS, k)` в applySyncData + тест на `__proto__`-ключ.

**P1 — целостность хранилища:**
5. T5-M1+T1-L2: перекомпоновать saveGameState (backup/IDB/cloud до full_save, per-key try).
6. T1-H1: паддинг id при synthetic-миграции (`sh01`) или санитайз с fallback-матчем по индексу + восстановление счётчика из season.snapshot.
7. T1-H2: инкремент stateGen при каждом saveGameState + e2e конфликт-сценарий.
8. T1-M3: onDone/badge при переполнении чанков; T5-M2: try вокруг top-level чтений; T2-M3: санитайз дат задач; T1-M4: sanitizeGoal при загрузке goals.

**P2 — продуктовая честность:**
9. T2-M1: платить/списывать goal.gold или убрать 💰 из тостов и GOAL_REWARDS; T2-M2: расширить dqProgress('gold') на все источники.
10. T5-M3: гейт screenShake; T3-M1: переписать чузер целей без HP; T1-M1: hard-delete карточки мимо бэкапа.

**P3 — доки/инфра:**
11. T6-M1/M2/M3/M4/M7: переписать §13/§15/§9/§8/§4.1 под v10 (по фактическому снапшоту storage.js:199-205).
12. T6-M6: добавить 5 недостающих команд в ci.yml; T6-M5: актуализировать AGENTS.md; T3-M2: решить судьбу 9 boss-jpg; T3-L3: aria-label на 10 «✕»; T4-M1: убрать force:true + retry в CI; чистка хандоффов (v61, заглушки-обман, a084b20/ea134f3).

**Решение за владельцем:** судьба img/bosses/ (T3-M2); принимать ли «стартовый ранг любой» как фичу (T6-M7) или править доки; внешний фон image.qwenlm.ai — оставить (мягкая деградация) или захостить локально.

---

## 6. Вердикт (оригинал, до исправлений)

Продукт функционально здоров (гейты V/F PASS, все 12 сюит зелёные, формулы едины, XSS/NaN чисто), но **синхронизационный контур хранилища — слабое место**: 4 из 6 HIGH-находок в одной области (cloud/transfer/multi-tab), и все они приводят к потере или устареванию прогресса игрока. P0-пункты (4 патча, суммарно ~десяток строк + тесты) закрывают краш эндгейма и все необратимые потери. Документация отстала от кода на 3-4 итерации — миграционный/туулинг-код по ней писать нельзя до T6-фиксов.

---

## 7. Статус исправлений (обновлено 2026-09-17, после аудита)

**Все блоки P0–P3 применены.** Верификация: `check:js` OK, юнит **184/184, 0 fail** [F]; полный гейт исполнителя: e2e 16/16, visual 5/5, симы 18+40+20+31+215 OK [F].

| Блок | Статус | Примечание |
|------|--------|------------|
| P0: T2-H1, T1-H4, T1-H3, T1-H5 | ✅ Исправлено | скобки тернарника; throne+dailyEvent+savedAt в buildSyncData; hasOwnProperty-гвард |
| P1: T1-H1, T1-H2, T5-M1, T1-M3, T5-M2, T2-M3, T1-M2, T1-M4 | ✅ Исправлено | padStart-id + fallback-матч; stateGen++; порядок записи IDB/cloud→full_save; onDone при капе; top-level try; safeTs; id задач; sanitizeGoal |
| P2: T2-M1, T2-M2, T5-M3, T1-M1 | ✅ Исправлено | goal.gold платится/списывается; goldGain(n,src) 7 мест; гейт screenShake; hard-delete карточки |
| Слой-баг («2 слоя») | ✅ Исправлено | диагноз: блокирующий слой = starterDeckModal z=400 (авто-show) и onboarding z=9999; декор-слои чисты (pointer-events:none). Фикс: backdrop-клик закрывает starter-модалку; гонка показа закрыта существующим in-function guard (FORGED.length===0) |
| UI: T3-M1, T3-L3, T4-M1, T3-M2 | ✅ Исправлено | чузер целей «-N HP»→«+N 💰»; aria-label на 10 «✕»; force:true убран из e2e; img/bosses/ (9 jpg) удалены |
| Кэш | ✅ | ?v=61→v62 (index.html + пины 2 тестов) |
| P3-доки: T6-M1–M7, ART_MANIFEST, ci.yml, AGENTS.md | ✅ Исправлено | §13/§15/§4.1/§2/§12/§16–18 переписаны под v10; SPEC §8→8352, §9 схема, +§Sync, +trade routes; ci.yml +5 шагов; AGENTS.md 12 скиллов; SESSION-2026-09-17.md создан |

**Осталось (осознанно):** LOW-хвосты (T1-L1/L2/L3, T2-L1–L3, T5-L1–L3) — не блокируют, в бэклоге; новые регресс-тесты e2e (конфликт multi-tab, «20/20 захвачено») — рекомендованы, не заведены; ничего не закоммичено — ожидает явной команды владельца.

{
  "name": "neurodeck-bot",
  "version": "0.1.0",
  "private": true,
  "description": "NeuroDeck Telegram notification backend - HTTP server (Node 22) talking to api.telegram.org directly via fetch. Self-contained: no SDK, minimal deps (express only).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/*.test.js",
    "ci": "npm test"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "express": "^4.21.2" },
  "license": "ISC"
}
